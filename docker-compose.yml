services:
  db:
    image: postgres:16-alpine
    container_name: dishboard_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dishboard_network

  redis:
    image: redis:7-alpine
    container_name: dishboard_redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - dishboard_network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: dishboard_backend
    command: >
      sh -c "
        python manage.py migrate &&
        python manage.py load_standard_foods /app/data/standard_foods.csv &&
        python manage.py runserver 0.0.0.0:8000 
      "
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - dishboard_network

  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: dishboard_celery
    command: celery -A dishboard_project worker -l info
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    depends_on:
      - backend
      - redis
    networks:
      - dishboard_network

  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: dishboard_celery_beat
    command: celery -A dishboard_project beat -l info
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    depends_on:
      - backend
      - redis
    networks:
      - dishboard_network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: dishboard_frontend
    command: npm run dev -- --host 0.0.0.0
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - dishboard_network

volumes:
  postgres_data:

networks:
  dishboard_network:
    driver: bridge